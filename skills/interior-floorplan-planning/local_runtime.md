# 运行

使用同安装根的 interior-html-modeling/scripts/engine；Python 路径和依赖见 [共用运行说明](../interior-html-modeling/local_runtime.md)。本入口仅输出 JSON，旧 trace/export 平面 HTML 不作为本 Skill 入口。

原图与输出只放来酷项目目录；读取原图需要宿主识图能力，脚本不宣称自动识图。原生平面方案图片使用宿主实际图像生成工具并附源图，不调用平台付费接口。记录真实耗时、图片与假设，不编造尺寸。
