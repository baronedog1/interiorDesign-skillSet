# HTML 表现选项

`backend-options.json` 只控制表现，不能改写 handoff。

- 天花从房间 polygon 生成，可显示或隐藏。
- 窗型只能在同一上游洞口内选择无框、固定、平开或推拉表现，不能新增、删除或改尺寸。
- 阳台栏杆、矮墙、开放边和落地玻璃直接读取 `boundaryFeatures[]`。开放边无实体，栏杆不是墙，玻璃不是新房间。
- 墙端编辑只产生可回交平面 Skill 的 patch，不能成为正式模型的第二套结构事实。
- 交互碰撞只用于人工编辑反馈；正式来源 placement 不通过碰撞自动搬动。
